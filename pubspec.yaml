name: sistema_ordenes
description: "Sistema de gestion de trabajos."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  pocketbase: ^0.18.0
  provider: ^6.1.1
  intl: ^0.19.0
  web: ^0.5.1
  esc_pos_utils: ^1.1.0
  esc_pos_printer: ^4.1.0
  win32: ^5.0.0
  ffi: ^2.1.0
  excel: ^4.0.0
  file_selector: ^1.0.0
  path: ^1.9.0

  cupertino_icons: ^1.0.6
  url_launcher: ^6.3.2
  fl_chart: ^0.68.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
